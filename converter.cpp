#include "converter.hpp"

namespace stat {
  // Converter from COO format to CSR
  auto converter_COO_to_CSR() {
  // auto mette in automatico se int, double etc, al momento idk cosa sia
    return boh;
  }
  // Converter from CSR format to COO
  auto converter_CSR_to_COO() {
    return boh;
  }
}
// per chiamare nel main scriverai stat::converter_COO_to_CSR(robe);
