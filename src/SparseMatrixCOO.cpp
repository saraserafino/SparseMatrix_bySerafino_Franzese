#include "SparseMatrixCOO.hpp"
#include <vector>

// the length of the following three arrays is nnz (number of non-zeros)
// the array values contains all the nonzero values
std::vector<double> values =
// the array rows of int contains their corresponding row indices
std::vector<int> rows = 
// the array cols of int contains their corresponding column indices
std::vector<int> cols =
